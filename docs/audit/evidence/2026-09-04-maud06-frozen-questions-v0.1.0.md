# A0 M-AUD-06 冻结题目

- 冻结日期：2026-09-04
- 出题方：独立会话（非主审）起草 7 题；董事会替换 3 题
- 标注说明：`[SETTER-DRAFTED]` = 独立出题方起草；`[BOARD-REPLACED]` = 董事会替换
- 通过线：**10/10**
- ⚠️ 答题者**不得**查看评分标准

## A0-Q01 [SETTER-DRAFTED]

同一项 Phase 3 变化，在 `B-REF`、`B-ASIS` 和当前代码中可能呈现三种不同状态。假设当前代码已经删除了 `B-ASIS` 中存在的未鉴权入口，为什么仍不能把 A4 对 `B-ASIS` 的历史结论改写为“当时已安全”？这三个时间边界应分别回答什么问题？

## A0-Q02 [BOARD-REPLACED]

A6 的订正版把 **26 个产品问题**与 **5 个审计治理阻断项（AG-01~AG-05）**分成两张表分别计数，明确不混算严重度。为什么必须分开？如果把它们合并成「31 个问题」按同一套 P0/P1/P2 排序，会导致什么后果？

## A0-Q03 [SETTER-DRAFTED]

A1 暂停了“HTTP 静态路由与运行时路由差集为零”的结论，并把 WebSocket 可达性保留为未知。什么新证据出现，才能推翻这两项判断？

## A0-Q04 [BOARD-REPLACED]

A6 台账对 M-01 的当前证据写的是「代表性 FACT；精确计数 SUSPENDED」——即**保留了根因、暂停了分母**。这两半为什么必须分开处置？如果只保留一个总数（例如「94 条写操作仅 10 条有归属校验」），会给后续改造计划带来什么具体误导？

## A0-Q05 [SETTER-DRAFTED]

机器人资产管理与机器人运动控制看起来都属于“机器人功能”，实际审计对象有什么根本差别？如果资产页面、接口和自动测试全部通过，能否据此认定运动控制和急停已经可用？

## A0-Q06 [SETTER-DRAFTED]

A4 认为审批设施没有完整进入真实执行路径。什么证据出现，才能推翻这一结论？该证据怎样证明批准、拒绝和重复操作都不会绕过或重复执行动作？

## A0-Q07 [SETTER-DRAFTED]

`E2_NOT_COLLECTED` 和 `E2_BLOCKED` 看起来都表示“没有 E2 证据”，实际差别是什么？如果 A5 如实标记了所有缺失证据，为什么 A5 报告可能获批，而生产能力仍不能判定通过？

## A0-Q08 [BOARD-REPLACED]

A6 的 0.1.0 版本存在两处错误：一是**声明 25 个 Master 而表中实为 26 行**，二是**把两套审批实现的属性写反了**（说「被实际使用的那套用内存队列」，实际用内存队列的那套是零消费者的死代码）。这两个错误的性质有什么根本不同？哪一个更危险，为什么？后来新增的机械检查脚本能防住其中哪一个、防不住哪一个？

## A0-Q09 [SETTER-DRAFTED]

A5 认为工作流配置文件不能证明 PostgreSQL 迁移、服务启动和端到端流程已经成功。什么证据出现，才能推翻“这些运行能力仍未被证明”的当前结论？

## A0-Q10 [SETTER-DRAFTED]

假设 A6 的问题归并已经计算正确、争议项已经裁决，当前代码也修复了若干高风险问题，为什么仍不能直接批准 R1？请给出从当前状态走到 R1 可被正式批准的必要顺序。

