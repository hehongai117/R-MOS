### A0-Q01

`B-REF`回答干预前怎样；`B-ASIS`回答审计时点怎样；当前代码回答此后发生了什么、修复是否经新基线复验。A4结论绑定`B-ASIS`，后来的删除只能改变当前状态，不能反写历史。

### A0-Q02

产品问题描述系统本身的缺陷，按人身、数据和交付影响分P0/P1/P2；AG项描述审计证据与批准链是否可信。混成31项会污染严重度和优先级，掩盖“系统已修但审计仍无效”或相反的状态。

### A0-Q03

HTTP需保存同一当前基线下两套独立原始清单，按“方法＋完整路径＋最终前缀”比较，解释差异后为零。WebSocket需同时给出动态路径人工核对、实际注册表及构建或运行连接证据，才能确定可达或不可达。

### A0-Q04

代表性代码已能证明根因存在；精确比例则依赖可复现的全量枚举，证据不足必须暂停。沿用“94/10”会把修复范围、工期和完成率建立在错误分母上，漏掉动态、新增或误分类入口，并制造虚假验收线。

### A0-Q05

资产管理处理机器人档案、可见范围、模型等信息；运动控制直接驱动设备，必须有命令契约、状态、安全联锁和急停。不能。资产页面、接口和测试通过，只证明资产链；控制与急停仍缺失，真机门禁仍受阻。

### A0-Q06

需从真实用户入口取得运行证据：动作先持久化为待批，批准后才执行；拒绝后执行数为零；重复批准、重试和并发只执行一次。审批、动作和结果还须由同一追踪标识串联并可回放，证明没有旁路。

### A0-Q07

`E2_NOT_COLLECTED`表示具备或可能具备采集条件，但尚未采集；`E2_BLOCKED`表示能力、批准或等价环境等前提不成立，当前不能采集。A5可因如实完整地标明边界而获批，但这些能力仍是未知或受阻，不能算生产通过。

### A0-Q08

“26写成25”是结构与算术不一致；审批属性写反是事实和因果判断错误。后者更危险，会保留错的实现、删除对的对象并漏掉真实旁路。机械脚本能核对行数、编号和严重度合计，不能判断哪套代码被真实调用。

### A0-Q09

需在获批的一次性等价环境中，对同一明确提交提供环境指纹、命令、时间、退出码和原始日志：从干净PostgreSQL完成迁移，服务实际启动且健康语义正确，再由浏览器或API跑通端到端流程，并记录清理结果。

### A0-Q10

先闭合A0门禁并以准确口令批准；再按A1至A6顺序增量重验、解决争议、建立当前代码新基线并逐阶段批准；A6批准后完成并批准R0；只有A6和R0均通过，才编制R1并以准确口令正式批准。