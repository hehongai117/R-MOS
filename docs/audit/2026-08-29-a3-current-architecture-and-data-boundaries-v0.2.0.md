# A3 当前架构与数据边界审计报告

- 版本：0.2.0
- 日期：2026-08-29
- 复核状态：RETURN FOR REVISION
- 事实基线：29d2a5889e3b320a3e777e3d8c19efbbe31c0294
- M-AUD-06：BLOCKED
- 替代关系：本文件保留可定位的代表性架构事实，暂停无法复现的全量数字和通过结论。

## 1. 本阶段应完成什么

依据[董事会方向指令](../plans/2026-08-26-rmos-complete-audit-and-modernization-board-directive-v0.2.0.md)，A3 要说明现有进程与部署拓扑、前后端边界、模块责任、调用方向、共享状态、数据所有权、重复模型、循环依赖、外部依赖、扩展点和删除边界，不在本阶段决定目标目录或新接口。

## 2. 实际提交材料

- [A3 历史架构证据](evidence/2026-08-27-a3-architecture-evidence-v0.1.0.md)
- 原报告中的共享状态 S-01～S-10、数据归属 D-01～D-13 和可替换边界 B-01～B-09。

历史证据文件包含大量人工说明和示意命令，但未保存生成全部精确计数的脚本与原始输出。因此可以核对代表性实例，不能复现所有分母。

## 3. 已验证的代表性事实

| 编号 | 结论 | 类型 | 边界 |
|---|---|---|---|
| A3-F-01 | 后端存在 API、服务、模型分层，前端存在多个页面目录与调用层。 | FACT | 说明当前结构，不评价目标结构。 |
| A3-F-02 | 存在模块级可变单例，承载审批、事件历史、任务状态或证据状态。 | FACT | 代表性实例可定位；“35 个”“8 个”之类全量数字暂停。 |
| A3-F-03 | 审批、回放、证据、角色、知识和 HTTP 调用存在多套实现或多处状态源。 | FACT | 具体分组数量在历史 A6 0.1.1 中曾实质订正，需逐组重验。 |
| A3-F-04 | 存在 API 端点直接写模型、脚本独占写入和运行期不可维护的数据对象。 | FACT | 代表性路径成立；所有写入点的全量分母不可复现。 |
| A3-F-05 | 存在 LLM 路由与 provider 间的循环依赖线索。 | FACT | 可作为结构问题登记，不据此决定重写。 |

## 4. 被暂停的精确结论

| 原结论 | 当前状态 | 原因 |
|---|---|---|
| 35 个模块级单例、8 个持可变业务状态 | SUSPENDED | 枚举脚本与原始输出未随证据保存，且后续又补出漏项。 |
| 16 张表由 API 端点直接写入 | SUSPENDED | 有代表性事实支持，但全量生成方法不可复现。 |
| 6 张表只由脚本写入 | SUSPENDED | 代表性对象可定位；完整搜索输入和排除规则未冻结。 |
| A1 每项功能都能定位到模块与数据 | UNKNOWN | A1 分母未正式闭合，无法计算全量映射率。 |
| 所有跨模块数据写入点有所有者 | FAIL | 原材料本身揭示端点直写、多写入者和内存状态。 |

## 5. 结论边界

- FACT：代表性模块、单例、模型、存储和依赖可由静态代码定位。
- INFERENCE：这些模式可能导致重启丢失、横向扩展不一致或责任分散；未做运行故障注入。
- UNKNOWN：部署环境中的进程数量、卷挂载、服务发现、实际依赖解析、数据库写入来源和运行时调用图。
- 本报告不批准目标目录、目标接口、模块重写或删除；这些属于 R1 和后续实施决策。

## 6. 门禁裁决

| 门禁 | 状态 |
|---|---|
| A1 每项功能映射到模块和数据 | UNKNOWN |
| 所有跨模块数据写入点有所有者 | FAIL |
| 重复模型、共享状态、不可替换边界登记 | CONDITIONAL：代表性登记成立，全量分母不足 |
| 不提前决定目标结构 | PASS |
| M-AUD-05 | FAIL |
| M-AUD-06 | BLOCKED |
| 准确批准口令 | FAIL：未找到 `确认 Audit A3` 原始记录 |

## 7. 最小修正要求与重开范围

1. 保存可执行的单例、写入点、数据所有者、循环依赖和重复实现枚举脚本及原始输出。
2. 冻结纳入/排除规则，对历史精确数字重新计算；无法枚举的部分标 UNKNOWN。
3. 逐项把 A1 功能映射到模块和数据，分母必须继承修正后的 A1。
4. 只重开精确分母与退出门禁；已定位的代表性根因无需重复发现。
5. 补 M-AUD-05、M-AUD-06 和准确批准记录。

## 8. 阶段最终裁决

**FAIL。代表性架构问题成立，但全量数据边界和所有者覆盖无法复现；需要定向重开分母和门禁。**
