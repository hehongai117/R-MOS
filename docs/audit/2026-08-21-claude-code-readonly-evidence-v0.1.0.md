# Claude Code 只读协作证据

- 版本：0.1.0
- 日期：2026-08-21
- 程序：`/opt/homebrew/bin/claude`
- 版本：2.1.220
- 边界：只允许 Read、Glob、Grep；不保存会话；禁止创建、修改或删除文件

## 1. 登录状态差异

| 检查环境 | 退出码 | 结果 | 裁决 |
|---|---:|---|---|
| Codex 隔离环境内 | 1 | `loggedIn: false` | 隔离环境无法读取用户登录信息，不能据此判定真实登录失败 |
| 隔离限制之外、同一 macOS 用户 | 0 | `loggedIn: true`，`authMethod: claude.ai` | 当前 Claude Code 登录有效 |

文档不记录邮箱、组织编号、令牌或其他凭据。

## 2. 真实只读样例

### Attempt 1：预算不足

- 模型：默认模型（实际为 Claude Opus 5）
- 预算上限：0.30 美元
- 退出码：1
- 实际费用：约 0.62031 美元
- 结果：模型请求已发起，但在返回最终答案前触发 `budget_exhausted`
- 失败处理：确认根因是固定上下文成本超过预算，不再把该结果误判为登录失败；后续改用 Sonnet 并提高受控预算
- 仓库影响：调用前后文件状态一致，零改动

### Attempt 2：登录与只读能力通过

- 模型：Claude Sonnet 5
- 预算上限：1.00 美元
- 退出码：0
- 实际费用：约 0.52557 美元
- 结果摘要：独立识别出 9 个缺失引用、Python 环境冲突，以及“重建验收章程、统一环境与开发记录、复验 Claude”的三个目标
- 仓库影响：调用前后 `git status --short --branch` 只有分支行，零文件改动
- 裁决：登录、读取、检索和返回结果均通过；写能力未开放、未测试，也不属于本任务目标

## 3. 陌生读者复核

- 模型：Claude Sonnet 5
- 预算上限：1.00 美元
- 退出码：0
- 实际费用：约 0.97922 美元
- 输入：最高规则、镜像、验收章程、测试计划、当前报告、验收矩阵和 Phase 0 审查文档
- 检查问题：事实源顺序、Python 环境、历史 PASS、生产启用、证据位置、Claude 状态、矛盾、悬空引用和过度结论
- 初次结论：FAIL，提出 4 项有效发现

| 发现 | 独立核对 | 处理 |
|---|---|---|
| Claude 成功调用缺少落盘证据 | 成立 | 新增本证据文件，并从审查索引链接 |
| `REL-BLOCK-01` 的定义文件未登记 | 成立 | 将部署回滚计划加入事实源和现行优先级，验收章程显式引用 |
| `TEST_PLAN.md` 仍有 14 个标题写成 PASS | 成立 | 全部改为 HISTORICAL |
| Python 检查项预填 ✅ 易被误读为环境就绪 | 成立 | 改成每次现场核对后选择 ✅/❌，未核对不得运行 Python |

完成修正后必须再次运行陌生读者复核；只有复核不再发现 P0/P1/P2 问题，才能把 DOC-RULE-001 写成 PASS。

## 4. 第二轮陌生读者复核

- 模型：Claude Sonnet 5
- 预算上限：1.20 美元
- 退出码：0
- 实际费用：约 0.73346 美元
- 结果：上一轮 4 项发现全部关闭；未发现新的 P0、P1 或 P2 问题
- 轻微提示：事实源登记表的“当前材料”仍保留修复前描述
- 处理：将该表更新为规则修复后的真实状态，并把原 9 个缺失引用明确标为 Phase 0 历史快照
- 裁决：陌生读者复核通过；最终提交仍须经过本地路径、镜像、编号、状态和差异检查
