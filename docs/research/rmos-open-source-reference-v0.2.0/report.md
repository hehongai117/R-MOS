# R0 开源参考架构研究纠正版总报告

- 版本：0.2.0
- 日期：2026-08-30
- 裁决：**RETURN FOR REVISION**

## 1. 一句话结论

旧版三个核心错误已经撤销、八域分母已经恢复、八份既有结果已经按固定来源重写，但候选发现、安全/许可证取证、评分资格和 A6 批准仍不满足，因此 R0 尚未真正完成，也不能作为 R1 的绑定输入。

## 2. 已完成的纠正

- 将 VDA 5050 从“急停模型”恢复为业务通信规范；
- 纠正不可取消动作的 FINISHED/FAILED 语义；
- 删除 openTCS LGPL 适用断言，以固定 `REUSE.toml` 为准；
- 用固定有意义提交和发布重做六个软件候选的 G4；
- 补齐八份结果的显式字段，未取得的证据均保留 UNKNOWN；
- 将两个规范对象从 OSS 100 分评分中分离；
- 恢复 D-01～D-08 和 26 个 A6 Master 的研究分母；
- 冻结 D-03、D-04 的下一轮评分校准；
- 明确六个软件候选目前都不具备数值评分资格和主/次参考资格。

## 3. 仍未完成

| 要求 | 当前结果 |
|---|---|
| 完整候选与淘汰记录 | FAIL：登记了当前候选分母，但逐域发现、分页、淘汰和饱和记录未完成 |
| 每项目结构化原始结果 | CONDITIONAL：八份旧对象已纠正；其他候选未研究 |
| 各域评分矩阵 | FAIL：资格矩阵完成；无候选通过全部硬门槛，数值评分未触发 |
| 每域 0–2 主参考 | PASS（边界事实）：当前八域均为 0；不代表搜索完成 |
| 借/不借边界 | CONDITIONAL：D-03/D-04 已补，其余域未研究 |
| 许可证与安全风险 | CONDITIONAL：旧错误已纠正，目标版本级 G2/G5 仍 UNKNOWN |
| R1 决策问题 | CONDITIONAL：已补跨域问题，但 A6 未批准，不具绑定性 |

## 3.1 五域候选发现进展

`evidence/2026-08-30-five-domain-candidate-discovery-v0.1.0.md` 已为 D-01、D-02、D-05、D-06、D-07 保存第一轮发现记录：23 条原样查询、20 个待深研或观察对象、去重/筛除说明和 41 个官方来源链接。该记录只覆盖公开结果首屏与官方目录入口，没有完成分页、搜索饱和、固定版本、§7.7 深度字段或 G1～G6 判定。

因此，五域状态从“没有候选发现记录”提升为“第一轮发现已留证”，但合格参考仍为 0，阶段门禁不变。其中 D-06 的备份恢复主责参考和 D-07 同时覆盖身份、对象授权、拒绝审计的参考仍未找到。

## 4. 范围传导

未经董事会批准，不再裁掉任何研究域。尤其是 M-01、M-02、M-06、M-13、M-18a 五个 P0 已重新进入 D-07、D-05、D-06 分母，但当前仍没有完成的外部参考结果。这是显式缺口，不是 PASS。

## 5. 阶段门禁

| 门禁 | 状态 | 原因 |
|---|---|---|
| 搜索达到路线饱和 | FAIL | 五域仅完成首屏第一轮发现，D-03/D-04 也无完整搜索日志；没有分页和饱和证据 |
| 取证完整 | FAIL | G2/G5、贡献者、响应样本和多个 §7.7 字段为 UNKNOWN |
| 评分可复现 | FAIL | 候选未过全部硬门槛，不能产生数值评分；双人评分未触发 |
| 淘汰原因充分 | FAIL | 没有完成候选评估，因此没有合法淘汰记录 |
| A6 可用 | BLOCKED | A6 0.2.0 为 RETURN FOR REVISION，M-14/M-19 仍 DISPUTED |
| R1 启动 | BLOCKED | 章程要求 A6 与 R0 均通过 |

## 6. 最小下一步

1. 先完成 A6 的治理闭环和 M-14/M-19 决定；
2. 按 D-01～D-08 逐域保存查询、分页、时间窗、候选加入/淘汰和饱和记录；
3. 对软件候选按目标版本补齐 G2/G5；
4. 只有通过全部门槛的候选才由主审和异源复核方独立评分；
5. 汇总差异、冻结每域 0–2 主参考和最多 2 次参考，再申请 R0 批准。
