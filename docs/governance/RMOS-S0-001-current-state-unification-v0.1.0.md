# RMOS-S0-001｜统一现状、冻结当前基线、生成唯一问题清单

- 版本：0.1.0｜日期：2026-09-05
- 主干阶段：**S0｜统一现状**（`docs/governance/RMOS_PROJECT_MAINLINE.md`）
- 主干任务编号：**RMOS-S0-001**
- 提交对象：董事会
- 执行者：Claude（主审）／Codex（异源取证）

---

## 0. 本文件解决什么

主干 §2 把 S0 的当前阻断写为「审计索引、阶段报告、董事会裁定和整改交接存在**状态时间差**；
当前基线与当前问题清单尚未统一」。本文件按主干 §2 要求的五项逐项提交。

**本文件不宣布阶段通过。** 主干 §4 规定只有董事会可以宣布阶段通过，
执行者不得把「文件完成」改写成「阶段通过」。

---

## 1. 唯一的当前工作区、分支、提交和环境摘要

| 项 | 值 |
|---|---|
| 工作区 | `/Users/xuhehong/Desktop/r-mos/.worktrees/phase3-auth-control-realtime`（git worktree，**不是主仓**） |
| 分支 | `audit/phase3-auth-control-realtime` |
| **冻结基线** | **`27df7c52711e0ee4441501ce304a6f60cd82c25c`** |
| 工作区状态 | clean（本文件写入前） |
| 是否 push | 是，`origin/audit/phase3-auth-control-realtime` |
| 相对 `B-ASIS` | **64 个提交**（`B-ASIS=29d2a5889e3b320a3e777e3d8c19efbbe31c0294`） |
| 后端测试 | **995 passed**（本机 PostgreSQL 可连时的完整结果） |
| 前端测试 | **518 passed / 2 skipped**，`tsc --noEmit` 无错误 |
| 后端解释器 | `/Users/xuhehong/Desktop/r-mos/r-mos-backend/venv/bin/python`（**唯一**，被审 worktree 无 venv） |
| 数据库 | 本地 PostgreSQL `localhost:5432/rmos`，Alembic head `20260904_m02_ownership`（单一 head） |

### 1.1 环境陷阱（复现必读）

```bash
cd /Users/xuhehong/Desktop/r-mos/.worktrees/phase3-auth-control-realtime/r-mos-backend
set -a; . /Users/xuhehong/Desktop/r-mos/r-mos-backend/.env; set +a
unset CORS_ORIGINS        # 该字段以环境变量形态存在时不是 JSON，pydantic-settings 拒绝解析
export DEBUG=true         # 否则 validate_production() 拒绝启动
/Users/xuhehong/Desktop/r-mos/r-mos-backend/venv/bin/python -m pytest -p no:warnings
```

- **不要额外传 `-q`**：`pytest.ini` 已有 `addopts = -q`，再传变 `-qq`，汇总行消失
- **不要传 `--timeout`**：未装 `pytest-timeout`

> **Codex 沙箱下会有 3 项失败**（`test_audit_query_indexes_exist`、
> `test_audit_trace_query_explain_uses_trace_index`、`test_skill_registry_migration_gate`），
> 原因是沙箱禁止连 `::1:5432`，报 `PermissionError: Operation not permitted`。
> **属环境限制，不是缺陷**；涉及 Alembic 实迁的验证必须由主审在本机复跑。

### 1.2 取证依据

环境指纹与逐项复比由**异源执行**（Codex）产出：
`docs/audit/evidence/2026-09-05-b-asis-to-head-drift-recomparison-v0.1.0.md`。
该报告刻意不倒改历史结论，只回答「历史结论能否继续描述当前 HEAD」。

---

## 2. 唯一的当前问题清单

历史目录仍为 A6 的 26 个 Master_ID，**编号不变、不重排**。
下表只给**当前技术事实**下的分类，不重算历史严重度分布（`P0=8、P1=11、P2=7` 是 B-ASIS 的治理事实）。

**分类计数自校验**：已解决 1 ＋ 部分解决 11 ＋ 仍存在 10 ＋ 待决定 4 ＝ **26**，
与 A6 唯一问题表的 26 个 Master_ID 一一对应，无遗漏、无重复计入。
（§2.4 的四项残余风险是**跨类的处置标注**，其中 M-06 已计入「部分解决」，
AG-02／G05／`auth.register` 不属 A6 的 26 项编号，故不参与上述计数。）

### 2.1 已解决（1）

| ID | 根因 | 当前事实 |
|---|---|---|
| M-05 | adapter 域入口无鉴权 | 入口已删除，能力保留。判定 `NO_LONGER_APPLICABLE`，需确认删除为正式产品决策 |

### 2.2 部分解决（11）

| ID | 根因 | 已做 | 未做 |
|---|---|---|---|
| M-01 | 写入口缺身份／归属 | 87 写入口中 83 有身份、37 有对象/作者保护；五表 + 三表补归属字段与迁移 | 其余入口需逐项区分「无需对象」「只有角色」「缺对象范围」 |
| M-02 | 业务身份未绑定认证身份 | 自述身份写端点 12→2，余 2 有书面判定 | 「83 有身份」不等于绑定证明，需全量语义复核 |
| M-03 | WS 无认证／无过滤 | 握手前认证、机器人可见性授权、用户维度定向 | `robot_id` 数据源过滤未实现（单 adapter 无多源数据，属新功能） |
| M-04 | 网关外入口 | 生产文档路由已关闭 | 根路径仍在网关外 |
| M-06 | 审批不在执行链 | 旧内存队列删除；持久化审批接入执行链 | **真实副作用仍为 stub，「批准即生效」不成立**（董事会已裁定维持现状） |
| M-10 | CI 重复配置吃掉 DEBUG | 配置重复键已修 | **需当前提交的真实 CI 运行记录才能正式关闭**（见 §3 冲突 C4） |
| M-12 | 租户隔离偏读路径 | 多个读写入口新增归属字段与守卫 | 维保审批仍未见学校范围校验；单对象 owner 不能代替 tenant |
| M-14 | 新旧实现并存 | 旧内存任务／审批接口删除，重复面缩小 | replay／metrics／evidence 分组仍在，需逐组冻结 |
| M-16 | 内存代替落库 | 旧 `approval_queue` 删除 | 15 张无应用写入表名单不变，其他内存状态仍在 |
| M-17 | 测试证明边界窄 | 用例 971→995，安全负向测试增加 | SQLite 基座仍在；58 个浅断言函数待重审 |
| M-21 | CI 门禁覆盖不完整 | M-10 配置已修 | 其他门禁与远程执行证据仍不完整 |

### 2.3 仍存在（10）

`M-07`（无运动控制／急停契约）、`M-08`（教学训练写能力无前端闭环，10 个域无写前端）、
`M-11`（unhealthy 仍返回 200）、`M-13`（角色多源；维保作者自批已拦，但 auditor 仍可批准/拒绝）、
`M-15`（**运行期本地文件未纳入持久卷**，见 §3 冲突 C2）、`M-18a`（备份恢复回滚缺失）、
`M-18b`（监控告警依赖治理缺失）、`M-20`（容器权限与构建上下文）、
`M-22`（任务终态写入责任未收口）、`M-23`（知识批准后未形成检索底料）。

> 这 10 项在本轮改造中**没有取得改变根因的新证据**，不是「未处理」，是「事实未变」。

### 2.4 明示接受的残余风险（4）

| 项 | 内容 | 裁定来源 |
|---|---|---|
| AG-02 / M-AUD-06 | 以八轮对抗式复核记录替代，**不得记为通过** | 2026-09-04 裁定 |
| A0 G05 | 无备用 P0 渠道，主渠道不可用即停审 | A0 门禁 |
| `auth.register` | 同校内学生自选挂靠教师，审计留痕为补偿控制而非消除 | 裁定 §9-4 |
| M-06 断点④ | 批准后执行为受控占位，agent 不产生写副作用 | 裁定 §10-1 |

### 2.5 待决定（4）

| ID | 待决内容 |
|---|---|
| M-09 | 前端悬空调用：本次改造造成的 3 条**已修**（本报告基线），存量 18 条待处置 |
| M-19 | 严重度仍 DISPUTED；且第 18 批新增 `_trace_owner_user_ids` 扩大了进程内状态面（判定 `CHANGED_WORSE`） |
| M-24 | 文档/指纹/实现漂移：64 提交、186 文件，本报告即为新基线输入；是否建自动漂移检查待定 |
| M-25 | 模块责任与目录边界，须待 S1 目标架构决策后才能冻结 |

---

## 3. 已发现的状态冲突及处理结果

### C1｜主干方向 vs 用户当轮要求（**已按主干处理**）

用户本轮提出「结束 A0–A6，继续 R0、R1」。主干 §2 将 A0–A6 认证链标为**暂停**，
唯一下一步是 RMOS-S0-001；主干 §5 任务准入规定「无法回答『对应哪个主干任务编号』时，不进入执行」。

**处理**：A0–A6 定向重开（7 条口令）、AG-03 P0 主备通道补证、R0 的 G2/G5 取证
**全部未执行**——三者均对应不上主干任务编号。已在本轮开始前向用户说明。
本轮唯一执行的是 RMOS-S0-001。

> 主干 §1：若其他文件与本文件的项目方向或当前状态冲突，以本文件为准并登记冲突。**此条即该登记。**

### C2｜M-15 编号指代两件不同的事（**已更正**）

- **A6 台账的 M-15**：「运行期本地文件未纳入持久卷」（P1，A3 来源，OPEN）
- **前一窗口交接文档的 M-15**：「跑测试污染 `data/knowledge_store.json`，每轮手动还原」

第 14 批修的是后者，并在 `DEVELOPMENT_LOG` 与交接文档中记为「M-15 关闭」——**该口径错误**。
测试污染修复**不等于**生产持久卷问题修复。

**处理**：本清单中 M-15 归入「仍存在」（§2.3）。测试污染问题不占用 A6 台账编号，
其修复事实保留在 `DEVELOPMENT_LOG` 第 14 批记录中。误用编号源自前一窗口，本轮沿用后由异源复比抓出。

### C3｜本轮改造自身造成的退化（**已修复**）

第 11 批删除 `/agent/v2/task*` 三条后端路由时，删了 approval 的 4 个前端函数，
**漏了 task 的 3 个**及孤儿类型 `TaskContext`，造成前端悬空调用由 15 条增至 21 条（M-09 `CHANGED_WORSE`）。

**处理**：本基线 `27df7c52` 已删除，`/agent/v2/task` 前端零残留。存量 18 条未动，列入 §2.5 待决。

### C4｜CI 从未在 GitHub 上真跑过（**保持开放**）

M-10 修复后 CI 未取得任何真实运行记录；本机无 `gh` CLI，主审无法读取 Actions 结果。
**处理**：M-10 保持「部分解决」（§2.2），董事会 2026-09-04 已裁定暂不核查。
**对外不得声称「CI 验证通过」。**

### C5｜三份文件的状态时间差（**本文件即为统一结果**）

`docs/audit/README.md`（A0–A6 状态表）、
`docs/handover/2026-09-04-...-v0.1.0.md`（改造第 18 批终态）、
`docs/plans/2026-09-03-board-decision-...`（三批裁定）三者时间点不同。

**处理**：本文件为 S0 阶段的唯一当前状态来源。三份历史文件**保留供追溯，不再作为当前状态依据**。
其中审计索引描述的是 B-ASIS 时期的被审系统，与当前代码的差异见漂移复比报告。

---

## 4. S0 通过条件逐项结果

主干 §3 定义 S0 通过条件四项：

| # | 条件 | 结果 | 依据 |
|---|---|---|---|
| 1 | 当前代码基线明确 | ✅ **达成** | `27df7c52`，clean，已 push；测试 995 / 518+2；Alembic 单一 head |
| 2 | 已知问题集中在一张当前清单 | ✅ **达成** | §2，26 个历史编号全部归入五类，无遗漏、无重排 |
| 3 | 冲突状态已统一或明确保留为未知 | ✅ **达成** | §3 五项冲突：C1/C2/C3 已处理，C4/C5 明确保留并写明理由 |
| 4 | 董事会确认唯一下一步 | ⬜ **待董事会** | 本文件即为该确认的输入 |

**第 1–3 项由执行者完成，第 4 项只有董事会可以完成。**

---

## 5. 下一阶段申请

前三项通过条件已达成，第四项待董事会确认。若董事会认可本文件，请回复准确口令：

```
确认主干阶段 S0 完成，进入 S1
```

### 5.1 董事会在批准前应注意的三点

1. **A0–A6 未通过，也未撤销。** 认证链处于暂停，不等于审计通过。
   若材料需对外用于投标、尽调、认证或合规，按路线 B 裁定 §4 须从 AG-02 重启认证链——
   届时本文件的基线与问题清单可直接作为重开输入，不必重做。
2. **§2.4 的四项残余风险是明示接受的，不是已解决的。**
3. **对外表述约束仍然有效**（裁定 §8、§10）：不得称「审计已通过」「审批闭环已建立」
   「批准即生效」「CI 验证通过」。

### 5.2 进入 S1 后的第一件事（供参考，非本文件裁定）

主干 §3 定义 S1 目标为「决定系统未来的整体结构」，通过条件是
「主要模块清楚、每个模块的职责和数据归属清楚、董事会批准总体方向」。

§2.5 的 `M-25`（模块责任与目录边界）与 §2.2 的 `M-14`（新旧实现并存）
是 S1 的直接输入——两者都在等目标架构决策才能收口。
